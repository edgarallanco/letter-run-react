var testFunction, test_one, test_two, startMe

global.test_one = 12;
global.test_two = 30;

/* function UpdateItems(itemName) {
  menuOpen.click();
  itemArray.forEach(function(el) {
    if (el.classList.contains('cc-just-collected-wf')){
      console.log(el.id)
      el.querySelector('.collected').classList.add('true');
    }
    el.classList.remove('cc-just-collected-wf');
    
});
  
  document.getElementById(itemName).classList.add('cc-just-collected-wf')
  itemLabel.innerText = document.getElementById(itemName).title;
  document.getElementById(itemName).querySelector('.uncollected').classList.add('hide');
} */
